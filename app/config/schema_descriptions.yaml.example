# Auto-generated by: python -m app.schema_enrichment
# Do NOT edit manually — regenerate from the live database.
#
# To generate:
#   python -m app.schema_enrichment              # full generation (stats + LLM descriptions)
#   python -m app.schema_enrichment --stats-only # stats only, skip LLM
#
# The real file (schema_descriptions.yaml) is gitignored because it
# contains sample values from the production database.

schema: Project_Master_Database
generated_by: app.schema_enrichment
tables:
  ExampleTable:
    description: "Example table description generated by LLM."
    columns:
      example_id:
        type: integer
        description: "Primary key."
      name:
        type: character varying
        description: "Display name."
        sample_values: [Example A, Example B, Example C]
    relationships:
      - "example_id -> OtherTable.example_id"

  Deliverable:
    description: "Individual deliverables produced for a client engagement. One engagement can have many deliverables."
    columns:
      deliverable_id:
        type: uuid
        description: "Primary key."
      engagement_id:
        type: uuid
        description: "FK to ClientEngagement.engagement_id."
      name:
        type: text
        description: "Short name of the deliverable."
        sample_values: [Cost Model, Final Report, Executive Dashboard]
      description:
        type: text
        description: "Optional detailed description of the deliverable."
      deliverable_type:
        type: text
        description: "Category of deliverable."
        sample_values: [Report, Model, Dashboard, Presentation, Tool]
      delivered_date:
        type: date
        description: "Date the deliverable was provided to the client."
      notes:
        type: text
        description: "Additional context or remarks."
    relationships:
      - "engagement_id -> ClientEngagement.engagement_id"
