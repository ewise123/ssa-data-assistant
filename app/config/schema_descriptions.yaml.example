# Auto-generated by: python -m app.schema_enrichment
# Do NOT edit manually — regenerate from the live database.
#
# To generate:
#   python -m app.schema_enrichment              # full generation (stats + LLM descriptions)
#   python -m app.schema_enrichment --stats-only # stats only, skip LLM
#
# The real file (schema_descriptions.yaml) is gitignored because it
# contains sample values from the production database.

schema: Project_Master_Database
generated_by: app.schema_enrichment
tables:
  ExampleTable:
    description: "Example table description generated by LLM."
    columns:
      example_id:
        type: integer
        description: "Primary key."
      name:
        type: character varying
        description: "Display name."
        sample_values: [Example A, Example B, Example C]
    relationships:
      - "example_id -> OtherTable.example_id"
